{
	"name": "@gyurielf/ci-test",
	"version": "2.0.3",
	"description": "The purpose is make an npm publish and a release on GH",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "prettier --write --plugin-search-dir=. .",
		"release": "changeset publish"
	},
	"publishConfig": {
		"access": "public",
		"directory": "/package"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@changesets/cli": "^2.26.1",
		"@changesets/get-github-info": "^0.5.2",
		"@changesets/types": "^5.2.1",
		"dotenv": "^16.0.3",
		"prettier": "^2.8.7"
	}
}
